# Frontend (Flutter)

Basic Flutter web client for the Virtual AI Patient platform.

- **Target Flutter version**: 3.41.3
- **Targets**: web (primary), plus mobile as needed.

The full Flutter project structure (including `pubspec.yaml`, `lib/`, and `web/`) should be created with `flutter create` using Flutter 3.41.3 in this `frontend/` directory.

